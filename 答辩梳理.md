# 星屿镇 AI Town 答辩梳理

## 1. 项目一句话介绍

这是一个 Vue + Node.js 的 Web AI 小镇游戏。玩家推进回合后，后端会结合 NPC 人设、当前位置、精力和回合时间，生成 NPC 的下一步行动；配置 DeepSeek 时使用真实 AI，未配置或调用失败时自动使用 Mock 规则，保证游戏始终可体验。

线上体验地址：<http://82.156.68.40:3001>  
代码仓库：<https://github.com/chn123123123/xingyu-ai-town>

---

## 2. 技术栈

| 层级 | 技术 | 用途 |
| --- | --- | --- |
| 前端 | Vue 3、TypeScript、Vite | 小镇页面、NPC 面板、回合报告、任务和响应式布局 |
| 地图 | HTML/CSS + Canvas | 静态可访问地图与动态游戏地图 |
| 后端 | Node.js、Express、TypeScript | API、NPC 决策校验、状态更新、任务判定 |
| AI | DeepSeek API（OpenAI Chat Completions 兼容格式） | NPC 行动选择与符合人设的对话 |
| 测试 | Vitest | API、决策安全、任务逻辑自动测试 |
| 部署 | Docker、Docker Compose、腾讯云 Lighthouse Ubuntu | 生产运行环境 |
| 版本管理 | Git、GitHub | 源码托管与版本记录 |

---

## 3. 功能概览

- 4 个地点：潮汐咖啡馆、萤火工坊、风铃公园、月光码头。
- 3 位 NPC：林默（机械师）、夏芽（咖啡师）、秋寻（旅行画家）。
- 推进回合：每回合前进 2 小时，NPC 行动和状态会变化。
- NPC 对话：玩家可与不同人设的 NPC 对话。
- 可解释报告：展示每位 NPC 的动作、理由、精力变化及 AI/Mock 来源。
- 相遇事件：NPC 到达同一地点时生成互动故事，记录到小镇手记。
- 小镇委托：由服务端统计的任务和徽章。
- 双地图：普通 DOM 地图和动态 Canvas 地图。
- AI/Mock 双模式：无 key 也能稳定演示。
- 持久化：服务器重启后仍保留小镇进度。

---

## 4. NPC 决策怎么实现（重点）

### 4.1 推进一回合的流程

```text
玩家点击“推进一回合”
→ 前端请求 POST /api/world/tick
→ 后端收集：人设、地点、精力、回合、允许行为和允许地点
→ AI 模式：调用 DeepSeek 请求结构化决策
→ Mock 模式/失败：用后端规则生成合法决策
→ 后端校验 action、target、reason
→ 更新 NPC 地点、精力、心情、行动记录
→ 检查 NPC 相遇与任务进度
→ 返回完整世界状态，前端刷新页面
```

### 4.2 AI 返回格式

AI 被要求返回 JSON，例如：

```json
{
  "action": "move",
  "target": "park",
  "reason": "工作结束后想去公园放松"
}
```

### 4.3 为什么安全

AI 的输出不会直接修改游戏，也不会执行代码。后端会检查：

- 动作只能是 `move`、`work`、`rest`、`socialize`。
- 目标只能是系统已有地点。
- `reason` 必须是有效、长度受限的文本。
- `move` 必须换地点；非移动行为必须留在当前位置。
- JSON 解析错误、超时、未知动作、未知地点都会自动降级为 Mock。

可回答：**AI 负责在受限范围内选择行动和生成理由；后端规则负责校验和真正执行。**

相关文件：

```text
server/ai.ts        # AI 调用、超时、JSON 解析、Mock 降级
server/decision.ts  # 决策白名单、语义校验、状态更新和相遇事件
server/app.ts       # API 路由、请求校验与错误处理
```

---

## 5. AI Mode 和 Mock Mode

### AI Mode

服务器 `.env` 配置 DeepSeek 后启用：

```env
AI_API_KEY=你的DeepSeek密钥
AI_BASE_URL=https://api.deepseek.com
AI_MODEL=deepseek-chat
AI_TIMEOUT_MS=15000
```

真实 AI 参与 NPC 决策和对话。

### Mock Mode

没有 key、AI 超时、网络失败或返回非法内容时启用。Mock 不是随机占位，而是根据 NPC 精力、回合和人物编号，稳定生成休息、工作、移动或社交行为。

这样老师即使不配置 key，也可以完整验收游戏。

---

## 6. 持久化是什么

持久化表示服务器或 Docker 容器重启后，游戏不会回到第 1 回合。

会保存：

- 当前回合和游戏时间
- NPC 所在地点、精力、心情和当前行为
- 最近行动记录
- NPC 相遇事件和小镇手记
- 小镇委托进度和已解锁徽章

实现方式：后端将状态保存到 `.data/world.json`，使用临时文件写入加原子重命名降低文件损坏风险；Docker Compose 使用命名卷保存 `.data`。

服务器已配置：

```env
PERSIST_WORLD=true
```

健康检查返回 `persistence: true`，代表持久化已开启。

---

## 7. Canvas 是什么

Canvas 是浏览器的绘图画布。普通页面常用很多 `div`、文字和按钮组成；Canvas 则是在一个区域内用 JavaScript 实时画图。

本项目中 Canvas 用于动态小镇地图：

- 绘制河流、道路、光点和地点。
- 绘制 NPC 标记。
- 根据回合变化实时更新 NPC 位置。
- 支持点击 NPC 查看人物信息。

它是提高游戏感和视觉表现的加分项；同时保留 DOM 地图，方便键盘操作和读屏访问。

---

## 8. 自动测试怎么做

测试框架是 Vitest，主要文件：

```text
server/app.test.ts
server/decision.test.ts
server/quest.test.ts
```

运行：

```bash
npm run check
```

这个命令依次执行：

```text
TypeScript 类型检查 → Vitest 自动化测试 → 生产构建
```

当前共 12 项测试，覆盖：

- 获取世界状态和推进回合。
- NPC 聊天请求校验、未知 NPC 的 404。
- 畸形 JSON 的 400。
- AI 返回非法动作或不存在地点时自动回退 Mock。
- 动作和地点是否符合语义。
- 世界重置。
- NPC 相遇事件。
- 任务累计、只完成一次和奖励逻辑。

例如测试会故意让 AI 返回不存在的“火星基地”，断言后端不会让 NPC 前往该地点，而是回退为合法 Mock 行动。

---

## 9. 安全要求对应说明

| 作业要求 | 本项目做法 |
| --- | --- |
| 提供目录和 README | README 有目录结构、技术栈、启动、AI、测试与部署说明 |
| 提供 `.env.example`，密钥不入库 | 有 `.env.example`；真实 key 仅存在服务器 `.env`，被 Git 忽略 |
| 校验 AI 输出，不执行任意代码 | action/target/reason 均由后端白名单和语义规则校验；模型文本不执行 |
| 前端和接口失败处理、自动测试 | 统一 API 错误处理；AI 失败降级 Mock；12 项 Vitest 测试 |
| 理解 AI 生成代码 | 可说明 `ai.ts`、`decision.ts`、`app.ts` 的职责和安全边界 |

---

## 10. 腾讯云部署流程

实际运行环境是腾讯云 Lighthouse Ubuntu，GitHub 只用于存放源码。

```bash
sudo apt-get update
sudo apt-get install -y docker.io docker-compose-v2 git
git clone --depth 1 https://github.com/chn123123123/xingyu-ai-town.git
cd xingyu-ai-town
cp .env.example .env
nano .env
sudo docker compose up -d --build
```

腾讯云防火墙放行规则：

```text
来源：0.0.0.0/0
协议：TCP
端口：3001
策略：允许
```

检查命令：

```bash
sudo docker compose ps
curl http://localhost:3001/api/health
```

线上地址：<http://82.156.68.40:3001>

---

## 11. 现场演示顺序（建议）

1. 打开线上地址，介绍地图、4 个地点和 3 位 NPC。
2. 点击一个 NPC，展示人物设定、精力、当前位置和行动记录。
3. 点击“推进一回合”，展示回合报告中的动作、理由、精力变化和 `AI MODE`。
4. 多推进几次，展示 NPC 移动、相遇事件和小镇手记。
5. 与不同 NPC 对话，说明对话符合不同人物设定。
6. 切换动态 Canvas 地图，说明这是加分项。
7. 展示小镇委托，说明任务由后端统计，不依赖前端临时状态。
8. 展示 README、`.env.example` 和测试命令。

---

## 12. 如何说明我与 AI 的协作

可直接这样回答：

> 我将 AI 作为结对开发助手，而不是直接生成后提交。先由我根据题目拆分前端、后端、NPC 决策、安全、测试和部署模块；AI 协助生成项目骨架、组件初稿、样式、测试草案和文档。我负责确定 NPC 人设、动作边界、数据结构和验收标准，并重点审查 AI 输出安全：模型只能在允许动作和地点中选择，所有结果都要经过后端校验，失败时回退 Mock。最后由我完成 DeepSeek 配置、Docker 部署、腾讯云防火墙配置、线上健康检查和功能验收。

一句话总结：

> AI 提高编码和排错效率，我负责需求判断、规则设计、安全边界、真实配置、部署和最终验收。
